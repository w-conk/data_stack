<script>import { getAttrs } from "../ctx.js";
export let asChild = false;
const attrs = getAttrs("grid-row");
</script>

{#if asChild}
	<slot {attrs} />
{:else}
	<tr {...$$restProps} {...attrs}>
		<slot {attrs} />
	</tr>
{/if}
