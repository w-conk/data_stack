import { desc, Query } from '@uwdata/mosaic-sql';
export const order = (q, orderConfig) => {
    for (const [key, value] of Object.entries(orderConfig)) {
        if (value) {
            // Asc
            q.orderby(key);
        }
        else {
            // Desc
            q.orderby(desc(key));
        }
    }
    return q;
};
//# sourceMappingURL=order.js.map