export const handleMaybePromise = (resolvedFunction, execFunction, errorHandler) => {
    try {
        const v = execFunction();
        if (v instanceof Promise)
            return v.then(resolvedFunction).catch((e) => {
                if (errorHandler)
                    return errorHandler(e);
                else
                    throw e;
            });
        return resolvedFunction(v);
    }
    catch (e) {
        if (errorHandler)
            return errorHandler(e);
        else
            throw e;
    }
};
//# sourceMappingURL=handleMaybePromise.js.map