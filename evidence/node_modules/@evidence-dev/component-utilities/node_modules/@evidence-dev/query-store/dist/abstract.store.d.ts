/// <reference types="svelte" />
import type { Readable } from 'svelte/store';
export type Subscriber<T> = (value: T) => unknown;
export declare abstract class AbstractStore<T> implements Readable<T> {
    #private;
    protected abstract value(): T;
    subscribe: (subFn: Subscriber<T>) => () => boolean;
    protected publish: () => void;
}
//# sourceMappingURL=abstract.store.d.ts.map